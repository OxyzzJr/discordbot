services:
  bot:
    build: .
    container_name: discord-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot_data:/data
    ports:
      - "5000:5000"

volumes:
  bot_data:
